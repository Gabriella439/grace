# Returns `true` if a `Real` is non-negative, and `false` otherwise
let not = ../bool/not.ffg

let negative = ./negative.ffg

let nonNegative
      : Real -> Bool
      = \n -> not (negative n)

in  nonNegative
