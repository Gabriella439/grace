# Returns `true` if a `Real` is non-positive, and `false` otherwise
let not = ../Bool/not.ffg

let positive = ./positive.ffg

let nonPositive
      : Real -> Bool
      = \n -> not (positive n)

in  nonPositive
