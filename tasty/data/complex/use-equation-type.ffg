forall (a : Alternatives) .
forall (b : Fields) .
forall (c : Type) .
  { x: Bool, b } ->
  { x: Bool, b } ->
  < L: Natural | a > ->
  c ->
    { field1: List { x: Bool, b }, field2: List < L: Natural | a > }
