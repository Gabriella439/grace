# This test verifies that defaults are correctly selected when the argument is
# missing, either because it is set to `null` or (if it is a record field)
# omitted from the record.
let f (x = 1) { y = false } = { x, y }

in  [ f null { }
    , f 2 { y: true }
    ]
