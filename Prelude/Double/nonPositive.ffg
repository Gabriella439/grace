# Returns `true` if a `Double` is non-positive, and `false` otherwise
let not = ../Bool/not.ffg

let positive = ./positive.ffg

let nonPositive
      : Double -> Bool
      = \n -> not (positive n)

in  nonPositive
